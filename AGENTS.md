# Repository instructions

## 선택적 참조 로딩

- 작업 시작 시 `REFERENCES.md`를 읽고 현재 작업 유형에 표시된 `필수` 문서만 추가로 읽는다.
- 관련 없는 `docs/evidence/`, `docs/tdd/`, 계획 문서를 일괄 로드하지 않는다. 필요한 과거 기록은 먼저 `rg`로 파일명을 좁힌다.
- 사용자가 특정 문서를 지정하면 라우팅 표와 무관하게 그 문서는 읽는다.

## 공통 판정 원칙

- 문서 기준선보다 Codex의 독립적인 기술 판단과 실제 실행 증거가 우선한다.
- 확인하지 못한 항목은 통과시키지 않고 `미확인`으로 분리한다.
- 테스트 수·빌드 성공·TODO 체크만으로 기능 완료를 선언하지 않는다.
- 동작 변경의 최종 보고에는 RED 명령·의도한 실패 이유, GREEN 결과와 영향 범위 검증을 포함한다.
