# Repository instructions

## 핵심·기본 기능 완성도 리뷰

- 핵심 기능, 기본 기능, 제품 준비도, 구현 완성도 또는 마일스톤 완료 여부를 리뷰할 때는 판단 전에 반드시 `docs/CORE-FEATURE-COMPLETENESS.md`를 읽고 참조한다.
- 문서의 기능 분류, 필수 게이트, 점수·근거 규칙과 보고 형식을 평가의 기준선으로 사용한다. `TODO.md` 체크 여부, 코드 존재, 테스트 개수 또는 빌드 성공만으로 완료를 선언하지 않는다.
- 이 문서는 보조 기준이다. Codex의 독립적인 기술 판단과 실제 증거가 항상 우선한다. 문서의 규칙이 현재 맥락에 부적절하거나 서로 충돌하면 더 엄격하고 타당한 판단을 적용하고, 최종 리뷰에 이탈 내용과 이유를 짧게 밝힌다.
- 확인하지 못한 항목은 추정해 통과시키지 말고 `미확인`으로 분리한다. 리뷰 범위, 실행한 검증, 실행하지 못한 검증, 남은 위험을 명시한다.
- 이 규칙은 리뷰·감사에 적용한다. 평범한 기능 구현이나 국소 버그 수정 작업마다 전체 완성도 평가를 강제하지 않는다.

## TDD 작업 규칙

- 동작을 추가·변경하거나 버그를 수정할 때는 구현 전에 `docs/TDD-WORKFLOW.md`를 읽고 RED → GREEN → REFACTOR 절차를 따른다.
- RED 단계에서 대상 테스트를 단독 실행해 의도한 이유로 실패하는지 확인한다. GREEN 단계에서는 같은 명령의 통과 결과를 확인하고, REFACTOR 뒤에는 영향 범위에 맞는 전체 검증을 실행한다.
- 최종 작업 보고에는 테스트 파일, RED 명령·실패 이유, GREEN 명령·결과를 포함한다. PR을 만드는 작업은 `docs/tdd/`에 증거 기록을 추가한다.
- `TDD RED` 같은 주석, 테스트 개수 또는 현재 통과 결과만으로 테스트 선행을 주장하지 않는다. RED를 재현하지 못한 기존 코드 보강은 `특성화 테스트`로 정확히 표시한다.
- 문서·서식·순수 설정 변경처럼 런타임 동작이 바뀌지 않는 작업은 예외로 할 수 있으나, 예외 이유를 작업 보고에 밝힌다.
