# 3D 인테리어 시뮬레이터 — 프로젝트 계획 (v1)

> 목표: 도면 이미지를 해석해 평면도 → 3D 공간을 생성하고,
> **실측(mm) 기반**의 바닥재·벽지·싱크대·가구·가전을 배치하는 웹 애플리케이션

---

## 1. 국내 레퍼런스 분석 (핵심 개념 차용)

| 서비스                                     | 핵심 컨셉                                                                                                                         | 우리가 차용할 것                                                                                           |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| **오늘의집 3D 인테리어** (아키스케치 엔진) | 주소 검색으로 도면 제공 / 실제 판매 가구 배치 / 2D⇄3D 즉시 전환 / 배치안 복사 비교 / 렌더링 · 파노라마                            | ① 2D/3D 동기화 편집 ② 배치안 A/B 복사 비교 ③ 스크린샷(렌더링)                                              |
| **아키스케치 (Archisketch)**               | 치수 적힌 평면도 이미지 → AI 딥러닝으로 벽·문·창·빌트인까지 3D 변환. 치수 없으면 규격(문=900mm) 기준 비율 추론. 표준 오차 50~80mm | ① 도면 이미지 업로드 → 구조화된 평면도 JSON 변환 ② 규격 추론 로직 ③ AI 결과는 반드시 사용자 보정 단계 거침 |
| **한샘 홈플래너 2.0**                      | 전국 아파트 3D 도면 + 건자재(바닥재 90종·벽지 80종·타일 150종) 시뮬레이션                                                         | ① 방별 바닥재/벽지 교체 ② 실제 유통 규격 타일/장판 크기                                                    |

**공통 설계 원칙 (업계 표준)**

- 모든 길이는 **mm 단위**로 내부 저장, 화면에는 cm/m 병기
- 도면 = 데이터(JSON). 2D와 3D는 같은 데이터의 다른 뷰일 뿐
- 가구는 "제품 실측(W×D×H)" + 장착 방식(floor/wall-mounted/ceiling) 속성
- AI 해석은 완벽하지 않음 → 항상 **사람이 보정하는 2D 에디터**가 필수

---

## 2. 기능 명세

### F1. 평면도 입력 — 3가지 경로

- **A. 이미지 업로드 + AI 해석**: 도면 사진 → Vision API → `FloorPlan JSON` 자동 변환 (벽/문/창문/방 라벨/치수)
- **B. 이미지 트레이싱**: 이미지를 배경에 깔고 기준선 스케일 지정 후 벽을 따라 그리기
- **C. 직접 그리기**: 빈 캔버스에서 벽 그리기 시작
- 세 경로 모두 동일한 `FloorPlan` 스키마로 수렴 → 이후 흐름은 하나

### F2. 2D 편집기

- 벽 추가/이동/삭제, 벽 두께 설정, 문·창문 배치(벽 위 스냅)
- 치수선 자동 표시 + 클릭해서 수치 직접 입력(실측 확정용)
- 격자 스냅(10cm), 방(Room) 영역 정의 → 방 이름/용도 태그

### F3. 3D 공간 생성

- Three.js: 벽(두께·높이 2400mm 기본), 바닥, 천장, 문(프레임+짐), 창문(유리+홈)
- 궤도 카메라(탑다운↔아이소메트릭) + 1인칭 워크스루 모드
- 2D에서 수정 → 3D 즉시 반영 (단일 소스 오브 트루스)

### F4. 제품 카탈로그 (실측 기반) ★핵심

- 카테고리: `바닥재 / 벽지 / 싱크대·주방 / 거실가구 / 침실가구 / 수납 / 가전 / 조명 / 욕실`
- 각 제품: 실측 W×D×H(mm), 색상 옵션, 장착 방식(`floor | wall-mount | ceiling | wall-attach`)
- 국내 실제 유통 규격 샘플 데이터 내장 (예: 시스마루 600장판, 소파 3인용 W2100, 김영임 900 싱크대 상부장 등)
- **사용자 정의 제품**: 실제 가진 가구 치수를 직접 입력해 등록 (오늘의집의 "사이즈 입력 자유" 개념)

### F5. 배치 & 조작

- 카탈로그 → 클릭/드래그로 방 안에 배치
- **벽 부착 스냅**(싱크대·붙박이장·상부장), 15° 회전 스냅, 겹침 충돌 빨간 하이라이트
- 높이 조절(벽걸이 TV, 선반), 다중 선택, 복제, 삭제, Undo/Redo
- 배치안 복사(Plan A/B) 후 나란히 비교

### F6. 마감재 시스템

- 방별로 바닥재·벽지 지정: 마루/장판/타일/도장 — 절차적 텍스처 생성(외부 에셋 최소화)
- 실규격 패턴 반복 (600×600 장판, 300×600 타일 등) → 스케일감까지 실측 재현

### F7. 저장 · 출력

- localStorage 자동저장 + 프로젝트 JSON 파일 내보내기/가져오기
- 고해상도 PNG 캡처(렌더링 대체), 카메라 프리셋

---

## 3. 기술 스택 (제안)

| 레이어    | 선택                                      | 이유                                            |
| --------- | ----------------------------------------- | ----------------------------------------------- |
| 빌드      | Vite + React + TypeScript                 | 빠른 HMR, 3D 생태계 성숙                        |
| 3D        | three.js + @react-three/fiber + drei      | 선언적 3D, OrbitControls/TransformControls 내장 |
| 2D 에디터 | SVG + 포인터 이벤트 (커스텀)              | 도면 스냅/치수선 제어에 최적                    |
| 상태      | zustand (+immer)                          | 3D 앱 표준 조합, Undo 스택 구현 용이            |
| AI 해석   | Anthropic Claude Vision API (어댑터 패턴) | 도면→JSON. 키 없으면 트레이싱 모드로 폴백       |
| 텍스처    | Canvas 절차 생성                          | 오프라인 동작, 저작권 걱정 없음                 |

## 4. 데이터 모델 (요약)

```ts
// 단위: 전부 mm
interface FloorPlan {
  unit: 'mm'
  wallHeight: number
  wallThickness: number
  walls: Wall[] // {id, a:{x,y}, b:{x,y}, thickness}
  openings: Opening[] // {id, wallId, type:'door'|'window'|'entry', offset, width, height, sill}
  rooms: Room[] // {id, name, polygon, floorMaterialId?, wallMaterialId?}
}
interface Product {
  id
  name
  brand?
  category
  dims: { w: number; d: number; h: number } // 실측 mm
  mount: 'floor' | 'wall' | 'ceiling' | 'wall-mount'
  snapToWall?: boolean
  colorways?: string[]
  shape?: ParamShape // 파라메트릭 형태 정의(박스/L자소파/침대 프레임 등)
}
interface Placement {
  id
  productId
  roomId?
  pos: { x: number; y: number; z: number } // y=바닥으로부터 높이
  rotY: number
  colorway?
  scaleOverrides?
}
```

## 5. 마일스톤

| 단계             | 산출물                                                       |
| ---------------- | ------------------------------------------------------------ |
| **M0** 셋업      | Vite+R3F 프로젝트, 폰트/UI 셸                                |
| **M1** 코어      | FloorPlan 스키마 + 샘플 아파트 평면도(국민평형) 내장         |
| **M2** 3D 렌더러 | 벽/바닥/천장/문/창문 생성 + 카메라                           |
| **M3** 2D 에디터 | 그리기/편집/치수 입력/트레이싱 모드                          |
| **M4** 배치 엔진 | 카탈로그 UI + 드래그 배치 + 스냅/충돌/Undo                   |
| **M5** 마감재    | 바닥재·벽지 적용, 절차 텍스처                                |
| **M6** AI 해석   | 이미지 업로드 → Claude Vision → FloorPlan 변환 + 보정 플로우 |
| **M7** 마무리    | 저장/내보내기, 배치안 비교, 스크린샷, 폴리시                 |

---

## 6. 리스크 & 대응

- **AI 도면 해석 정확도**: 어느 회사든 50~80mm 오차 → "AI 초안 + 사람 보정" 플로우로 설계
- **가구 3D 모델 품질 vs 용량**: GLTF 파일 수백 MB 대신, 파라메트릭 프리미티브(박스 조합)로 실측 형상 재현 → 추후 GLTF 확장 가능
- **브라우저 성공**: 인스턴스 메싱(InstancedMesh), 그림자 옵션 토글
